const main = () => {
    console.log("Welcome to Modern Warfare Trilogy Online!");

    // Load additional data or initialize components as needed
};

document.addEventListener("DOMContentLoaded", main);